// 다음 중 set을 만드는 방법으로 올바른 것을 모두 고르시오.

/* 
1)  var x = {1, 2, 3, 5, 6, 7};    // 일반적인 배열 
2)  var x = {};                    // 객체를 생성하는 리터럴 방식 
3)  var x = new Set('javascript');
4)  var x = new Set(range(5));     // 알수없는 메서드 방식 
5)  var x = new Set(); 
*/

// 중복되지 않는 데이터를 저장하는 데이터 구조 = set
// 3, 5
